import type { Metadata } from "next";

export const metadata: Metadata = { title: "License — DIVE" };

// Placeholder — the read-only license / capacity view lands later in this PR.
export default function LicensePage() {
  return <h1 className="text-2xl font-semibold">License</h1>;
}
